version: '3.8'

services:
  # PostgreSQL
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=silicon
      - POSTGRES_PASSWORD=silicon123
      - POSTGRES_DB=silicon_lounge
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U silicon -d silicon_lounge"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - silicon-network

  # Redis
  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 2gb --maxmemory-policy allkeys-lru --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - silicon-network

  # API 服务
  api:
    build:
      context: ../
      dockerfile: apps/api/Dockerfile
    environment:
      - NODE_ENV=production
      - PORT=8080
      - WS_PORT=8081
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://silicon:silicon123@postgres:5432/silicon_lounge
      - FRONTEND_URL=http://localhost:3000
    ports:
      - "8080:8080"
      - "8081:8081"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - silicon-network
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  # Web 前端
  web:
    build:
      context: ../
      dockerfile: apps/web/Dockerfile
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://localhost:8080
      - NEXT_PUBLIC_WS_URL=http://localhost:8081
    ports:
      - "3000:3000"
    depends_on:
      - api
    networks:
      - silicon-network
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  # Nginx 负载均衡
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - web
    networks:
      - silicon-network

volumes:
  redis-data:
  postgres-data:

networks:
  silicon-network:
    driver: bridge